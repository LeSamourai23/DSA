#include <iostream>
#include <stdio.h>
#include <conio.h>

int main()
{
    system("cls");

    int arr[] = {64, 34, 25, 12, 22, 11, 90};
    int n = sizeof(arr) / sizeof(arr[0]);

    //Funtion(arr, n);

    for (int i = 0; i <= n-1; i++)
    {
        std::cout << arr[i] << " ";
    }

    return 0;
}